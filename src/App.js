import React from "react";
import Router from "./router/Router";
import { useSelector } from "react-redux";




const App = () => {
  return <Router />;
  // 기본 페이지 구성을 router에서 관리하도록 했습니다.
};

export default App;